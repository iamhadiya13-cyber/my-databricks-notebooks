SCEN_TASK_NO in {1}

select nvl(
	(select to_char(last_run_dt,'dd-mm-yy')  
	from log_table1 
	where 
	pkg_name = 'Dept_pkg' )
,sysdate) 
from dual
SCEN_TASK_NO in {10}


SCEN_TASK_NO in {20}


SCEN_TASK_NO in {30}

drop table ODI_SRC.C$_0FILTER purge  
SCEN_TASK_NO in {40}

create table ODI_SRC.C$_0FILTER
(
	DEPARTMENT_ID	NUMBER(4,0) NULL,
	DEPARTMENT_NAME	VARCHAR2(30) NULL,
	MANAGER_ID	NUMBER(6,0) NULL,
	LOCATION_ID	NUMBER(4,0) NULL,
	LAST_UPD_DT	DATE NULL
)
NOLOGGING  
SCEN_TASK_NO in {50}

select	
	SRC_DEPARTMENTS_1.DEPARTMENT_ID	AS DEPARTMENT_ID,
	SRC_DEPARTMENTS_1.DEPARTMENT_NAME	AS DEPARTMENT_NAME,
	SRC_DEPARTMENTS_1.MANAGER_ID	AS MANAGER_ID,
	SRC_DEPARTMENTS_1.LOCATION_ID	AS LOCATION_ID,
	SRC_DEPARTMENTS_1.LAST_UPD_DT	AS LAST_UPD_DT
from	ODI_SRC.SRC_DEPARTMENTS SRC_DEPARTMENTS_1
where	(1=1)
 and (SRC_DEPARTMENTS_1.LAST_UPD_DT >= TO_DATE('#TEST.V_Dept', 'DD-MM-YY'))
  
insert  into ODI_SRC.C$_0FILTER
(
	DEPARTMENT_ID,
	DEPARTMENT_NAME,
	MANAGER_ID,
	LOCATION_ID,
	LAST_UPD_DT
)
values
(
	:DEPARTMENT_ID,
	:DEPARTMENT_NAME,
	:MANAGER_ID,
	:LOCATION_ID,
	:LAST_UPD_DT
)  
SCEN_TASK_NO in {60}


BEGIN
DBMS_STATS.GATHER_TABLE_STATS (
    ownname =>	'ODI_SRC',
    tabname =>	'C$_0FILTER',
    estimate_percent =>	DBMS_STATS.AUTO_SAMPLE_SIZE
);
END;
  
SCEN_TASK_NO in {70}


SCEN_TASK_NO in {80}

drop table ODI_TRG.I$_TRG_DEPARTMENTS  
SCEN_TASK_NO in {90}

create table ODI_TRG.I$_TRG_DEPARTMENTS
(
	DEPARTMENT_ID	NUMBER(4,0) NULL,
	DEPARTMENT_NAME	VARCHAR2(30) NULL,
	MANAGER_ID	NUMBER(6,0) NULL,
	LOCATION_ID	NUMBER(4,0) NULL,
	LAST_UPD_DT	DATE NULL,
	IND_UPDATE		VARCHAR2(1 CHAR)
)  
SCEN_TASK_NO in {100}

insert into	ODI_TRG.I$_TRG_DEPARTMENTS
(
	DEPARTMENT_ID,
	DEPARTMENT_NAME,
	MANAGER_ID,
	LOCATION_ID,
	LAST_UPD_DT,
	IND_UPDATE
)
select 	
	FILTER_A.DEPARTMENT_ID,
	FILTER_A.DEPARTMENT_NAME,
	FILTER_A.MANAGER_ID,
	FILTER_A.LOCATION_ID,
	FILTER_A.LAST_UPD_DT,
	'I' IND_UPDATE
from	ODI_TRG.C$_0FILTER FILTER_A
where	(1=1)
  
